module.exports = {
    routes: app => {
        // app.get('/users', async function (req, res) {
        //     try {
        //         const response = await User.find();
        //         res.json(response);
        //     } catch (e) {
        //         res.json([]);
        //     }
        // })
    }
}
